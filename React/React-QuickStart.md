# React-QuickStart

## 条件渲染

无特殊语法；使用编写JavaScript代码时相同的技术

## 渲染列表

依赖JavaScript功能呈现组件列表

```javascript
const products = [

  { title: 'Cabbage', id: 1 },

  { title: 'Garlic', id: 2 },

  { title: 'Apple', id: 3 },

];

//map函数将产品数组转换为<li>项

const listItems = products.map(product =>

  <li key={product.id}>

    {product.title}

  </li>

);



return (

  <ul>{listItems}</ul>

);
```

<li>具有key属性，对于每一项都应该传递一个字符串或者数字，以在同级项目中唯一标识该项目



## 相应事件

在组件内声明事件处理函数来响应事件

```react
//组件
function MyButton() {

  function handleClick() {

    alert('You clicked me!');

  }



  return (
  
    <button onClick={handleClick}>

      Click me

    </button>

  );

}
```

`onClick={handleClick}` 末尾没有括号！不要调用事件处理函数：您只需将其传递下去即可。当用户单击按钮时，React 将调用您的事件处理程序。

| 你要做什么                 | 写法                                      |
| -------------------------- | ----------------------------------------- |
| **让函数在点击时执行**     | `onClick={函数名}` （无括号）             |
| **让函数在页面加载时执行** | `函数名()` （有括号，但通常不是你想要的） |

## 更新屏幕

组件记住一些信息并显示它的功能

```react
//第一步：导入useState

import { useState } from 'react';

//声明状态变量
function MyButton() {

  const [count, setCount] = useState(0);
  // ...
```

 `useState` 内有两个变量：当前状态 (`count` )，以及允许您更新它的函数 (`setCount` )。可以起任何名称，但约定是写 `[something, setSomething]` ..



第一次显示按钮时，count是0，因为我将0传给useState()。当我想更改状态时我需要调用setCount()，并把新值传给它。单击此按钮将增加计数器：。

```react
function MyButton() {

  const [count, setCount] = useState(0);



  function handleClick() {

    setCount(count + 1);

  }



  return (

    <button onClick={handleClick}>

      Clicked {count} times

    </button>

  );

}
```



多次渲染同一个组件，每个组件都会获得自己的状态==>

**同一个组件被多次使用时，每个实例都有独立的状态。**

**🎯 为什么这个特性很重要？**

1. **可复用性**：你可以放心地多次使用同一个组件（比如 ``、``），不用担心状态互相干扰。
2. **隔离性**：每个组件管理自己的数据，符合“单一职责”原则。
3. **可预测性**：点击某个按钮只影响它所在的组件，不会意外改变其他地方。

## 使用钩子Hooks

### 定义

以 `use` 开头的函数称为 Hooks 。 

`useState`是React提供的内置Hook。

您可以在 API 参考中找到其他内置 Hook，或者组合Hook编写自己的Hook。

### 顶部调用原则

只能在组件顶部调用Hooks，如果您想在条件或循环中使用 `useState`，请提取一个新组件并将其放在那里。

React Hooks 必须在组件的顶层调用（不能在条件、循环、嵌套函数中）

**为什么必须顶部调用**

- React 依赖 **Hook 调用的顺序** 来管理状态。

- 如果你在循环/条件中调用 `useState`，每次渲染时 Hook 的数量或顺序可能变化。

- 这会导致 React **混淆状态属于哪个变量**，引发 bug（比如状态错乱、报错等）。

- React 通过 **“Hook 调用顺序”** 来匹配每次渲染的状态：

  - 第一次渲染：调用第 1 个 `useState` → 分配状态 A
  - 第二次渲染：必须也在相同位置调用第 1 个 `useState` → 找到状态 A

  如果在条件中调用，第二次渲染时可能跳过某个 `useState`，导致后续所有状态都错位！

  > 🔗 官方比喻：**Hook 就像按顺序排列的插槽，不能跳着用。**

### ✅ 正确做法：提取新组件

把需要状态的逻辑**封装到一个独立的子组件中**，然后在循环/条件中使用这个子组件。

```react
function Counter() {
  const [count, setCount] = useState(0); // ✅ 合法
  return <button onClick={() => setCount(count + 1)}>{count}</button>;
}

function App({ showCounter }) {
  return (
    <div>
      {showCounter ? <Counter /> : <p>计数器已隐藏</p>}
    </div>
  );
}
```

```react
// 👇 提取新组件：每个 Item 都有自己的状态
function Item({ name }) {
  const [count, setCount] = useState(0); // ✅ 在组件顶层，合法！

  return (
    <div>
      <p>{name}: {count}</p> 
          // 这行啥意思
      <button onClick={() => setCount(count + 1)}>+1</button>
    </div>
  );
}

function App() {
  const [items] = useState(['苹果', '香蕉']);

  return (
    <div>
      {/* ✅ 在 map 中渲染子组件 */}
      {items.map((item, index) => (
        <Item key={index} name={item} />
      ))}
    </div>
  );
}
```



#### 为什么这样可行？

- 每个 组件 是一个**独立的组件实例**。
- 每个实例都在自己的顶层调用 `useState`，符合 Hook 规则。
- 每个 组件 拥有**自己独立的状态**（如前面讲过的“多个计数器”例子）。

### ✅ 总结

| 场景               | 错误做法                 | 正确做法                            |
| ------------------ | ------------------------ | ----------------------------------- |
| **循环中需要状态** | 在 `map` 里写 `useState` | 提取子组件，在子组件中用 `useState` |
| **条件中需要状态** | 在 `if` 里写 `useState`  | 提取子组件，用条件渲染组件          |
| **核心原则**       | 违反 Hook 规则           | 遵守“Hook 必须在顶层调用”           |



## 组件之间共享数据

有时需要组件来共享数据并始终一起更新

例如：要使两个 `MyButton` 组件显示相同的 `count` 并一起更新，您需要将状态从各个按钮**“向上”移动**到包含所有按钮的最接近的组件。

**让多个子组件（`MyButton`）共享同一个父组件（`MyApp`）的状态和事件处理逻辑，从而实现“同步更新”。**



这正是 React **单向数据流（Unidirectional Data Flow）** 的经典范例：

> **数据从父组件流向子组件（通过 props），子组件通过回调函数通知父组件变更。**

![image-20251201235341556](C:\Users\鳌\AppData\Roaming\Typora\typora-user-images\image-20251201235341556.png)

### 步骤

#### 1.上移

将状态从MyButton上移到MyApp

```react
export default function MyApp() {
// 更新计时器
  const [count, setCount] = useState(0);// 👈 状态在父组件中

  function handleClick() {
    setCount(count + 1); // 👈 事件处理也在父组件中
  }

  return (
    <div>
      <h1>Counters that update separately</h1>
      <MyButton />
      <MyButton />
    </div>
  );
}

function MyButton() {
  // ... we're moving code from here ...
}
```

#### 2.将状态向下传递

将状态连同共享点击处理程序一起从 `MyApp` 向下传递到每个 `MyButton` 。您可以使用 JSX 大括号将信息传递给 `MyButton`，就像您之前使用 <img> : 等内置标签所做的那样。

像这样传递下来的信息称为 props 。现在 `MyApp` 组件包含 `count` 状态和 `handleClick` 事件处理程序，并将它们作为 props 传递给每个按钮。

```react
export default function MyApp() {
  const [count, setCount] = useState(0);

  function handleClick() {
    setCount(count + 1);
  }

  return (
    <div>
      <h1>Counters that update together</h1>
          //重点
      <MyButton count={count} onClick={handleClick} />
      <MyButton count={count} onClick={handleClick} />
          // 重点
    </div>
  );
}
```

#### 3.更改组件

更改 `MyButton` 以读取您从其父组件传递的 props：。

```react
function MyButton({ count, onClick }) {
  return (
    <button onClick={onClick}>
      Clicked {count} times
    </button>
  );
}
```



#### 理解

1.父组件 `MyApp` 拥有状态和事件处理函数

- `count` 是**共享状态**（所有按钮都显示这个值）。
- `handleClick` 是**共享的点击逻辑**（点任意按钮，都让 `count` +1）。

2.通过 JSX 大括号 `{}` 把数据和函数“传下去”

- `count={count}` → 把当前的 `count` 值作为 **prop** 传给 `MyButton`
- `onClick={handleClick}` → 把 `handleClick` 函数作为 **prop** 传给 `MyButton`

3.子组件 `MyButton` 接收并使用这些 props

- 它**不管理自己的状态**，只负责**展示数据**（`count`）和**响应点击**（调用 `onClick`）。
- 所有“智能”（状态和逻辑）都在父组件 `MyApp` 中。

### 💡 这种模式的好处

1. **状态集中管理**：容易调试、维护。
2. **组件间通信**：子组件可以通过调用父组件传来的函数来“通知”父组件更新状态。
3. **复用性强**：`MyButton` 变成了“纯展示组件”（也叫 **受控组件**），可以在任何地方复用。

### export default

#### 作用

在一个 JavaScript（或 React）文件中，指定一个“默认导出”的值（比如函数、类、对象等），让其他文件能通过简洁的 `import` 语法来使用它。

> **`export default` 让你可以把当前文件的“主角”（通常是主组件、主函数或主配置）暴露出去，供其他模块导入使用。**

#### 🆚 对比

**命名导出（`export`） vs 默认导出（`export default`）**

| 特性         | `export default`             | `export`（命名导出）           |
| ------------ | ---------------------------- | ------------------------------ |
| 数量限制     | 每个文件只能有一个           | 可以有多个                     |
| 导入语法     | `import 自定义名 from '...'` | `import { 原名 } from '...'`   |
| 是否可重命名 | 是（自由命名）               | 需用 `{}`，可 `as` 重命名      |
| 典型用途     | 导出主组件/主函数            | 导出工具函数、常量、辅助组件等 |

```react
// utils.js
export const PI = 3.14;          // 命名导出
export function formatDate() { } // 命名导出

function mainLogic() { }         // 私有函数
export default mainLogic;        // 默认导出
```

```react
// 使用
import mainLogic, { PI, formatDate } from './utils';

// 或重命名
import doSomething, { PI as piValue } from './utils';
```

#### ✅ 总结

| 问题                          | 答案                                           |
| ----------------------------- | ---------------------------------------------- |
| `export default` 是干什么的？ | 指定模块的默认导出项                           |
| 能导出什么？                  | 函数、类、对象、字符串、数字等任何值           |
| 导入时要注意什么？            | 用 `import 名字 from '路径'`，名字可自定义     |
| React 中怎么用？              | 用来导出组件文件的主组件（如 `App`, `Button`） |
| 和普通 `export` 有什么区别？  | 默认导出更简洁，一个文件只能有一个             |

